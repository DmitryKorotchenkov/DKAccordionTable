Pod::Spec.new do |spec|
    spec.name = 'DKAccordionTable'
    spec.version = '0.9.0'
    spec.platform = :ios
    spec.source = { :git => 'https://github.com/DmitryKorotchenkov/DKAccordionTable.git' }
    spec.source_files = 'Classes'
    spec.requires_arc = true
end